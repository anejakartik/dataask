OPENAI_API_KEY=sk-your-key-here
DATAASK_MODEL=gpt-4o-mini
DATAASK_CORS_ORIGINS=*

# Set to 1 for a key-free demo — returns hand-written SQL for the example
# questions (good for CI, sponsor demos, and offline development).
DATAASK_USE_MOCK=0
